#ifndef MICROCHECK_HTTP_UTILS_H
#define MICROCHECK_HTTP_UTILS_H

#include <stdbool.h>

/**
 * Check if a string contains CRLF characters.
 */
bool contains_crlf(const char *);

#endif // MICROCHECK_HTTP_UTILS_H
